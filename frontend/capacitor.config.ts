import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.jnoguert.guessthecountry',
  appName: 'Guess the Country',
  webDir: 'dist',
}

export default config
